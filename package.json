{
  "name": "hakanumutbey-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build && node scripts/build-games.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "serve": "npx serve dist -l 3000"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^6.4.3"
  }
}
